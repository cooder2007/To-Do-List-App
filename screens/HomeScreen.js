// ─── Momentum App — HomeScreen (screens/ alias) ───────────────────────────────
// This is a re-export shim for teams that prefer the screens/ convention.
// The authoritative implementation lives in app/index.js (Expo Router).
export { default } from '../app/index';
